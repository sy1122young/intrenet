currently in production 
app.py > line 233 to retrive folder for windows (set to mac by default)
login password and user name
test test

local-files must be located on desktop

installs
python -m pip install cs50
pip install Flask-Session
pip install mysql-connector-python 

to run non devalopment server
python -m flask --app run --debug